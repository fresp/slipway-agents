/**
 * Placeholder model resolution handler for Part 0.
 *
 * OpenCode API used: none yet. The legacy plugin simply assigns
 * agents[name].model when present and does not resolve category or fallback
 * chains. Part 2 will add proactive model-chain helpers without changing the
 * plugin export shape.
 *
 * Remaining gaps: resolveModel and resolveFallbackChain are intentionally not
 * implemented in Part 0 to preserve behavior.
 */

export {};
